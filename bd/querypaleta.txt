ALTER TABLE  `ubicacion` ADD  `ocupado` TINYINT NULL DEFAULT  '0' AFTER  `devolucion` ;
INSERT INTO `pyme_prueba_standar_ceal`.`subseccion` (`opt_subseccion`, `archivo_tpl`, `archivo_php`, `cod_seccion`, `descripcion`) VALUES ('entradapaleta', 'entradapaleta_nuevo.tpl', 'entradapaleta_nuevo.php', '109', 'Nueva Entrada Almacen');
INSERT INTO `pyme_prueba_standar_ceal`.`correlativos` (`campo`, `formato`, `contador`, `descripcion`) VALUES ('id_ticket', '000000000', '1', 'Correlativo de id de ticket');
ALTER TABLE  `kardex_almacen_detalle` ADD  `etiqueta` INT NULL ;
ALTER TABLE  `kardex_almacen` ADD  `ticket_entrada` VARCHAR( 10 ) CHARACTER SET utf8 COLLATE utf8_general_ci NOT NULL ;




//no es seguro
ALTER TABLE  `calidad_almacen` ADD  `prescintos` VARCHAR( 255 ) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL ;
ALTER TABLE  `calidad_almacen` ADD  `id_seguridad` INT NOT NULL ,
ADD  `id_aprobado` INT NOT NULL ,
ADD  `id_receptor` INT NOT NULL ,
ADD  `nro_contenedor` VARCHAR( 50 ) CHARACTER SET utf8 COLLATE utf8_general_ci NOT NULL
ALTER TABLE  `calidad_almacen_detalle` ADD  `elaboracion` DATE NULL AFTER  `vencimiento` ;

